import React from 'react';
import setting from '../../../config/defaultSettings';

const Footer: React.FC = () => {
  return <div className="text-center">{setting.title}</div>;
};

export default Footer;
